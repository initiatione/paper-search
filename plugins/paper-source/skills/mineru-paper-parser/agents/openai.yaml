interface:
  display_name: "MinerU Paper Parser"
  short_description: "Parse local PDFs with MinerU into Paper Source artifacts"
  default_prompt: "Use $mineru-paper-parser to parse local PDFs into Paper Source source artifacts."
